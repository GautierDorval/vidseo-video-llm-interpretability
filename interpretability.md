# Interpretability

This compatibility file remains for older links.

The maintained equivalents now are:

- `content/en/technical.md`
- `llms.txt`
- `data/capabilities.json`

Short version:
VidSEO reduces the need for machines to guess what a video says by exposing transcript text as native HTML.
